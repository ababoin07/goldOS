# goldOS

Welcome on goldOS's NON-official repo. CPU specifications.md and VM.py are AI generated. I'm only focused on c compiler for now.

##vm.py use:

You've to run on the vm.py's directory with python on your computer : "python vm.py {compiled_file}.bin". For now, that's not useful because there's not yet any possibility to create a .bin compatible file.
